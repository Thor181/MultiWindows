<Window x:Class="MultiWindows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MultiWindows"
        xmlns:gif="http://wpfanimatedgif.codeplex.com"
        mc:Ignorable="d"
        Title="MainWindow"
        Height="450"
        Width="800"
        LocationChanged="Window_LocationChanged"
        SizeChanged="Window_SizeChanged"
        DataContext="{Binding RelativeSource={RelativeSource Self}}">
    <Canvas x:Name="MoveMe">
        <Button Height="15"
                Width="15"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Padding="0"
                Click="Button_Click">+</Button>
        <!--<Image x:Name="ImagePlace"
               gif:ImageBehavior.AnimatedSource="{Binding ImageS}"
               Source="{Binding ImageS}"></Image>-->
        <Image x:Name="ImagePlace"
               Source="{Binding ImageS}" >
            
        </Image>
    </Canvas>
</Window>
